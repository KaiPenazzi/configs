name = "hypr"

bashrc = [
  "if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then",
  "    exec Hyprland",
  "fi",
]

[packages.pacman]
packages = ["hyprland", "hyprlock", "hyprpaper", "wl-clipboard", "xdg-desktip-portal-hyprland"]

[packages.aur]
packages = ["hyprshot"]
