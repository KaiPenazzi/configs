name = "hypr"

bashrc = [
    "if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then",
    "    exec Hyprland",
    "fi",
]

[packages]
pacman = [
    "hyprland",
    "hyprlock",
    "hyprpaper",
    "wl-clipboard",
    "xdg-desktop-portal-hyprland",
]
aur = ["hyprshot"]
